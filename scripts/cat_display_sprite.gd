extends Sprite2D


# Called when the node enters the scene tree for the first time.
#func _ready() -> void:
	## Set viewporte texture path
	#var viewport_texture = get_parent().get_parent().get_node("SubViewport").get_texture()
	#self.texture = viewport_texture
	
